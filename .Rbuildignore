^LICENSE\.md$
^\.github$
^_pkgdown\.yml$
^docs$
^pkgdown$
^\.agents$
^CLAUDE\.md$
# Built by src/Makevars during a local install. R CMD build already drops
# src/*.o and src/*.so, but not a static archive, so a local check otherwise
# ships one and reports it as a WARNING.
^src/libzucrypt\.a$
^tests/consumer$
