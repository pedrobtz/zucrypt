% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{crypt_info}
\alias{crypt_info}
\title{Report what this build of zucrypt contains}
\usage{
crypt_info()
}
\value{
A list with elements:
\describe{
\item{\code{version}}{the \code{zucrypt} package version, as a \code{package_version}.}
\item{\code{abi_version}}{the C ABI version published to \code{LinkingTo}
consumers, as an integer. \code{0} while the interface is still moving;
it becomes \code{1} at the first release.}
\item{\code{algorithms}}{the digest algorithms this build provides, which
is exactly the set \code{crypt_hash()} and \code{crypt_hmac()} accept.}
\item{\code{vendored}}{a data frame with one row per vendored source, giving
its \code{source} name and the \code{version} the compiled library reports.}
\item{\code{build_flags}}{a named list of compile-time choices worth being
able to see from R: the operating-system random source that was
selected, and whether hardware acceleration is compiled in.}
}
}
\description{
Describes the installed package: its version, the C ABI it publishes, the
algorithms it provides, and the vendored cryptographic backend it was
compiled against.
}
\details{
Everything here is read from the compiled library, never from
\code{tools/vendor/manifest.tsv}. The manifest is maintainer tooling and is not
installed, so it could only describe the tree a build was made \emph{from} --
which is the one thing a user holding a binary package cannot check.
}
\examples{
info <- crypt_info()
info$version
info$algorithms
info$vendored

}
