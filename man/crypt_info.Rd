% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{crypt_info}
\alias{crypt_info}
\title{Report what this build of zucrypt contains}
\usage{
crypt_info()
}
\value{
A list with elements:
\describe{
\item{\code{version}}{the \code{zucrypt} package version, as a \code{package_version}.}
\item{\code{vendored}}{a data frame with one row per vendored source, giving
its \code{source} name and the \code{version} the compiled library reports.}
\item{\code{random_backend}}{the operating-system random source selected at
compile time. No function in this version of the package consumes
randomness; the backend requires the source to exist at link time.}
}
}
\description{
Describes the installed package: its own version, and the vendored
cryptographic backend it was compiled against.
}
\details{
Everything reported here is read from the compiled library, never from
\code{tools/vendor/manifest.tsv}. The manifest is maintainer tooling and is not
installed, so it can only describe the tree a build \emph{was made from} -- which
is the one thing a user with a binary package cannot check.
}
\section{Stage of development}{

This is a placeholder. The full \code{crypt_info()} described in the design
returns \code{abi_version}, the supported \code{algorithms} and \code{build_flags} as well,
and those arrive with the functions they describe. Nothing in this list will
change meaning when they do.
}

\examples{
info <- crypt_info()
info$version
info$vendored

}
